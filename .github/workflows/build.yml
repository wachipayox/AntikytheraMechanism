name: Native Artifact Push Smoke Test

on:
  push:
    branches:
      - agent/native-artifact-push

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Smoke test
        run: echo "native artifact workflow is runnable"

name: Validate detached empty native bounds

on:
  push:
    branches:
      - 'agent/validate-detached-empty-native-bounds-final'

permissions:
  contents: write

concurrency:
  group: validate-detached-empty-native-bounds-${{ github.ref }}
  cancel-in-progress: true

jobs:
  gametest:
    if: github.actor != 'github-actions[bot]'
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        include:
          - {label: core-1, create: 'false', simulated: 'false'}
          - {label: core-2, create: 'false', simulated: 'false'}
          - {label: simulated-1, create: 'true', simulated: 'true'}
          - {label: simulated-2, create: 'true', simulated: 'true'}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
      - uses: gradle/actions/setup-gradle@v4
      - shell: bash
        run: |
          mkdir -p probe
          set +e
          timeout --signal=TERM 17m ./gradlew --no-daemon runGameTestServer -Pinclude_create=${{ matrix.create }} -Pinclude_simulated=${{ matrix.simulated }} --console=plain > probe/${{ matrix.label }}.log 2>&1
          echo "$?" > probe/${{ matrix.label }}.status
          exit 0
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: detached-bounds-${{ matrix.label }}-${{ github.sha }}
          path: probe/*
          if-no-files-found: error

  publish:
    if: always() && github.actor != 'github-actions[bot]'
    needs: [gametest]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/validate-detached-empty-native-bounds-final
      - uses: actions/download-artifact@v4
        continue-on-error: true
        with:
          pattern: 'detached-bounds-*-${{ github.sha }}'
          path: ci-final-detached-bounds
          merge-multiple: true
      - shell: bash
        run: |
          mkdir -p ci-final-detached-bounds
          {
            echo "validated_sha=${GITHUB_SHA}"
            for f in ci-final-detached-bounds/*.status; do
              [ -e "$f" ] || continue
              echo "$(basename "$f" .status)=$(cat "$f")"
            done
          } > ci-final-detached-bounds/summary.txt
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git pull --rebase origin agent/validate-detached-empty-native-bounds-final
          git add ci-final-detached-bounds
          git diff --cached --quiet && exit 0
          git commit -m "Record final detached bounds validation"
          git push origin HEAD:agent/validate-detached-empty-native-bounds-final
